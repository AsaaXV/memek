import { Doujin } from './doujin';
import { SearchResult } from './search';
import { SortMethods } from './constants';
export declare class API {
    options: {};
    constructor(options?: {});
    private fetch;
    doujinExists(id: number | string): Promise<boolean>;
    fetchDoujin(id: number | string): Promise<Doujin | null>;
    fetchHomepage(page?: string | number, sort?: SortMethods): Promise<SearchResult>;
    search(query: string, page?: string | number, sort?: SortMethods): Promise<SearchResult>;
    searchByTagID(id: number | string, page?: string | number, sort?: SortMethods): Promise<SearchResult>;
    searchRelated(id: number | string, page?: string | number): Promise<SearchResult>;
    randomDoujinID(): Promise<number>;
    randomDoujin(): Promise<Doujin>;
}
export declare class nhentaiAPIError extends Error {
    response: Record<string, unknown>;
    url: string;
    constructor(response: Record<string, unknown>, url: string);
}
